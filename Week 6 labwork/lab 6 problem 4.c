
#include<stdio.h>
int main()
{
  int num;
  printf("Enter the length of array:");
  scanf("%d",&num);
  int a[num];
  for(int i=0;i<num;i++)
  {
      scanf("%d",%a[i]);
  }
   for(int i=0;i<num;i++)
   {
       printf("a[%d]=%d\n",i,a[i]);
   }
return 0;
}
